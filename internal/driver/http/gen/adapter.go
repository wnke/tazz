// Code generated by oapi-codegen. DO NOT EDIT.
package gen

import (
	"context"
	"encoding/json"
	"net/http"

	"github.com/doordash-oss/oapi-codegen-dd/v3/pkg/runtime"
	"github.com/go-chi/chi/v5"
)

// ServiceInterface defines the service interface for business logic.
type ServiceInterface interface {
	// CreateDevice Create a device
	CreateDevice(ctx context.Context, opts *CreateDeviceServiceRequestOptions) (*CreateDeviceResponseData, error)
	// ListDevices List devices
	ListDevices(ctx context.Context, opts *ListDevicesServiceRequestOptions) (*ListDevicesResponseData, error)
	// GetDevice Get a device by id
	GetDevice(ctx context.Context, opts *GetDeviceServiceRequestOptions) (*GetDeviceResponseData, error)
	// UpdateDevice Update a device by id
	UpdateDevice(ctx context.Context, opts *UpdateDeviceServiceRequestOptions) (*UpdateDeviceResponseData, error)
	// DeleteDevice Delete a device by id
	DeleteDevice(ctx context.Context, opts *DeleteDeviceServiceRequestOptions) (*DeleteDeviceResponseData, error)
}

// HTTPAdapter adapts the ServiceInterface to HTTP handlers.
// This struct is generated and should not be modified.
type HTTPAdapter struct {
	svc             ServiceInterface
	errHandler      OapiErrorHandler
	jsonBodyDecoder runtime.JSONBodyDecoderFunc
}

// NewHTTPAdapter creates a new HTTPAdapter wrapping the given service.
// If errHandler is nil, OapiDefaultErrorHandler is used.
func NewHTTPAdapter(svc ServiceInterface, errHandler OapiErrorHandler) *HTTPAdapter {
	if errHandler == nil {
		errHandler = &OapiDefaultErrorHandler{}
	}
	return &HTTPAdapter{svc: svc, errHandler: errHandler, jsonBodyDecoder: runtime.DecodeJSONBody}
}

// CreateDevice handles POST /devices
func (a *HTTPAdapter) CreateDevice(w http.ResponseWriter, r *http.Request) {
	ctx := r.Context()
	opts := &CreateDeviceServiceRequestOptions{}
	opts.RawRequest = r

	// Parse request body
	defer r.Body.Close()
	var body CreateDeviceBody
	if err := a.jsonBodyDecoder(r.Body, &body); err != nil {
		a.errHandler.HandleError(w, r, http.StatusBadRequest, OapiHandlerError{
			Kind:        OapiErrorKindDecode,
			OperationID: "CreateDevice",
			Message:     err.Error(),
		})
		return
	}
	opts.Body = &body

	// Call business logic
	resp, err := a.svc.CreateDevice(ctx, opts)
	if err != nil {
		code := http.StatusInternalServerError
		if resp != nil && resp.Status != 0 {
			code = resp.Status
		}
		a.errHandler.HandleError(w, r, code, err)
		return
	}

	// Apply custom headers from response
	if resp != nil && resp.Headers != nil {
		for k, v := range resp.Headers {
			for _, val := range v {
				w.Header().Add(k, val)
			}
		}
	}

	// Determine status code
	status := 201
	if resp != nil && resp.Status != 0 {
		status = resp.Status
	}
	if w.Header().Get("Content-Type") == "" {
		w.Header().Set("Content-Type", "application/json")
	}
	w.WriteHeader(status)
	if resp != nil && resp.Body != nil {
		_ = json.NewEncoder(w).Encode(resp.Body)
	}
}

// ListDevices handles GET /devices
func (a *HTTPAdapter) ListDevices(w http.ResponseWriter, r *http.Request) {
	ctx := r.Context()
	opts := &ListDevicesServiceRequestOptions{}
	opts.RawRequest = r

	// Parse query parameters
	queryParams := &ListDevicesQuery{}
	query := r.URL.Query()
	if queryParamLimitStr := query.Get("limit"); queryParamLimitStr != "" {
		queryParamLimit, err := runtime.ParseString[int](queryParamLimitStr)
		if err != nil {
			a.errHandler.HandleError(w, r, http.StatusBadRequest, OapiHandlerError{
				Kind:          OapiErrorKindParse,
				OperationID:   "ListDevices",
				Message:       err.Error(),
				ParamName:     "limit",
				ParamLocation: "query",
			})
			return
		}
		queryParams.Limit = &queryParamLimit
	}
	if queryParamOffsetStr := query.Get("offset"); queryParamOffsetStr != "" {
		queryParamOffset, err := runtime.ParseString[int](queryParamOffsetStr)
		if err != nil {
			a.errHandler.HandleError(w, r, http.StatusBadRequest, OapiHandlerError{
				Kind:          OapiErrorKindParse,
				OperationID:   "ListDevices",
				Message:       err.Error(),
				ParamName:     "offset",
				ParamLocation: "query",
			})
			return
		}
		queryParams.Offset = &queryParamOffset
	}
	opts.Query = queryParams

	// Call business logic
	resp, err := a.svc.ListDevices(ctx, opts)
	if err != nil {
		code := http.StatusInternalServerError
		if resp != nil && resp.Status != 0 {
			code = resp.Status
		}
		a.errHandler.HandleError(w, r, code, err)
		return
	}

	// Apply custom headers from response
	if resp != nil && resp.Headers != nil {
		for k, v := range resp.Headers {
			for _, val := range v {
				w.Header().Add(k, val)
			}
		}
	}

	// Determine status code
	status := 200
	if resp != nil && resp.Status != 0 {
		status = resp.Status
	}
	if w.Header().Get("Content-Type") == "" {
		w.Header().Set("Content-Type", "application/json")
	}
	w.WriteHeader(status)
	if resp != nil && resp.Body != nil {
		_ = json.NewEncoder(w).Encode(resp.Body)
	}
}

// GetDevice handles GET /devices/{id}
func (a *HTTPAdapter) GetDevice(w http.ResponseWriter, r *http.Request) {
	ctx := r.Context()
	opts := &GetDeviceServiceRequestOptions{}
	opts.RawRequest = r

	// Parse path parameters
	pathParams := &GetDevicePath{}
	pathParamIDStr := chi.URLParam(r, "id")
	pathParams.ID = pathParamIDStr
	opts.PathParams = pathParams

	// Call business logic
	resp, err := a.svc.GetDevice(ctx, opts)
	if err != nil {
		code := http.StatusInternalServerError
		if resp != nil && resp.Status != 0 {
			code = resp.Status
		}
		a.errHandler.HandleError(w, r, code, err)
		return
	}

	// Apply custom headers from response
	if resp != nil && resp.Headers != nil {
		for k, v := range resp.Headers {
			for _, val := range v {
				w.Header().Add(k, val)
			}
		}
	}

	// Determine status code
	status := 200
	if resp != nil && resp.Status != 0 {
		status = resp.Status
	}
	if w.Header().Get("Content-Type") == "" {
		w.Header().Set("Content-Type", "application/json")
	}
	w.WriteHeader(status)
	if resp != nil && resp.Body != nil {
		_ = json.NewEncoder(w).Encode(resp.Body)
	}
}

// UpdateDevice handles PATCH /devices/{id}
func (a *HTTPAdapter) UpdateDevice(w http.ResponseWriter, r *http.Request) {
	ctx := r.Context()
	opts := &UpdateDeviceServiceRequestOptions{}
	opts.RawRequest = r

	// Parse path parameters
	pathParams := &UpdateDevicePath{}
	pathParamIDStr := chi.URLParam(r, "id")
	pathParams.ID = pathParamIDStr
	opts.PathParams = pathParams
	// Parse request body
	defer r.Body.Close()
	var body UpdateDeviceBody
	if err := a.jsonBodyDecoder(r.Body, &body); err != nil {
		a.errHandler.HandleError(w, r, http.StatusBadRequest, OapiHandlerError{
			Kind:        OapiErrorKindDecode,
			OperationID: "UpdateDevice",
			Message:     err.Error(),
		})
		return
	}
	opts.Body = &body

	// Call business logic
	resp, err := a.svc.UpdateDevice(ctx, opts)
	if err != nil {
		code := http.StatusInternalServerError
		if resp != nil && resp.Status != 0 {
			code = resp.Status
		}
		a.errHandler.HandleError(w, r, code, err)
		return
	}

	// Apply custom headers from response
	if resp != nil && resp.Headers != nil {
		for k, v := range resp.Headers {
			for _, val := range v {
				w.Header().Add(k, val)
			}
		}
	}

	// Determine status code
	status := 200
	if resp != nil && resp.Status != 0 {
		status = resp.Status
	}
	if w.Header().Get("Content-Type") == "" {
		w.Header().Set("Content-Type", "application/json")
	}
	w.WriteHeader(status)
	if resp != nil && resp.Body != nil {
		_ = json.NewEncoder(w).Encode(resp.Body)
	}
}

// DeleteDevice handles DELETE /devices/{id}
func (a *HTTPAdapter) DeleteDevice(w http.ResponseWriter, r *http.Request) {
	ctx := r.Context()
	opts := &DeleteDeviceServiceRequestOptions{}
	opts.RawRequest = r

	// Parse path parameters
	pathParams := &DeleteDevicePath{}
	pathParamIDStr := chi.URLParam(r, "id")
	pathParams.ID = pathParamIDStr
	opts.PathParams = pathParams

	// Call business logic
	resp, err := a.svc.DeleteDevice(ctx, opts)
	if err != nil {
		code := http.StatusInternalServerError
		if resp != nil && resp.Status != 0 {
			code = resp.Status
		}
		a.errHandler.HandleError(w, r, code, err)
		return
	}

	// Apply custom headers from response
	if resp != nil && resp.Headers != nil {
		for k, v := range resp.Headers {
			for _, val := range v {
				w.Header().Add(k, val)
			}
		}
	}

	// Determine status code
	status := 204
	if resp != nil && resp.Status != 0 {
		status = resp.Status
	}
	w.WriteHeader(status)
}
