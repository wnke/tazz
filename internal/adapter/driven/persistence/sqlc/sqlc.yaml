version: "2"
sql:
  - engine: "sqlite"
    schema:
      - "schema/schema.sql"
    queries:
      - "queries/"
    gen:
      go:
        package: "db"
        out: "gen"
        sql_package: "database/sql"
